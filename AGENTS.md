# Frontend agent instructions

This is the frontend application.

Related backend workspace root: ../aplikasi-backend

For full-stack tasks:
- Ask the main agent to delegate backend work to a backend subagent.
- Frontend subagents may edit only this frontend repository.
- Read the backend API implementation before creating integration code.
- Do not edit unrelated workspace folders.
- Verify endpoint, method, request payload, response, and error format.